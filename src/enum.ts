export enum ViewMode {
  Canvas = "canvas",
  Html = "html",
  Json = "json",
}

export enum SizeEnum {
  Small = "small",
  Medium = "medium",
  Large = "large",
  Huge = "huge",
  Mini = "mini",
}

export enum ExportType {
  JSON = "JSON",
  HTML = "HTML",
}

export enum ScreenViews {
  DESKTOP = "Desktop",
  MOBILE = "Mobile",
}
